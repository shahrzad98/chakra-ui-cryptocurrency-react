import { defineMessages } from "react-intl";

export const scope = "app.containers.footer";

export default defineMessages({
  copyright: {
    id: `${scope}.copyright`,
  },
  terms: {
    id: `${scope}.terms`,
  },
  contact: {
    id: `${scope}.contact_us`,
  },
  help: {
    id: `${scope}.help`,
  },
  live: {
    id: `${scope}.live`,
  },
  home: {
    id: `${scope}.home`,
  },
  services: {
    id: `${scope}.services`,
  },
  about: {
    id: `${scope}.about`,
  },
  CareerOpportunities: {
    id: `${scope}.CareerOpportunities`,
  },
  

});
