// import { selectBlurredModalDomain } from '../selectors';

describe('selectBlurredModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
