/*
 * BlurredModal Messages
 *
 * This contains all the text for the BlurredModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BlurredModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BlurredModal container!',
  },
});
