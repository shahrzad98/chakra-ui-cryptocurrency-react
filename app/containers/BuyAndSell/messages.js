import { defineMessages } from "react-intl";

export const scope = "app.containers.BuyAndSell";

export default defineMessages({
    buy: {
        id: `${scope}.buy`,
    },
    Paymentfrom: {
        id: `${scope}.Paymentfrom`,
    },
    

    
});
