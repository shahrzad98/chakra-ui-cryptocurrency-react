import { defineMessages } from 'react-intl';

export const scope = 'app.containers.password';

export default defineMessages({
  mobileno: {
    id: `${scope}.mobileno`,
  },
  recoverMethod: {
    id: `${scope}.recoverMethod`,
  },
  totpMethod: {
    id: `${scope}.totpMethod`,
  },
  securityQuestionMethod: {
    id: `${scope}.securityQuestionMethod`,
  },
});
