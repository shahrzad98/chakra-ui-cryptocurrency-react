import { defineMessages } from 'react-intl';

export const scope = 'app.containers.drawer';

export default defineMessages({
  price: {
    id: `${scope}.price`,
  },
  about: {
    id: `${scope}.about`,
  },
  loginmenu: {
    id: `${scope}.loginmenu`,
  },

  start: {
    id: `${scope}.start`,
  },
  
});
