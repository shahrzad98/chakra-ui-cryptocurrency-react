import styled from 'styled-components';

const Select = styled.select`
  font-size: 12px;
  line-height: 1em;
  border-style: none;
  border-radius: 0 0 20px 20px;
  padding: 5px 15px;
  background: #fff;
  box-shadow: 0 9px 12px -5px rgba(0, 0, 0, 0.2);
  cursor: pointer;
`;

export default Select;
