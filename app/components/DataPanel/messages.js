/*
 * DataPanel Messages
 *
 * This contains all the text for the guide section
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.guide';

export default defineMessages({
  home: {
    id: `${scope}.home`,
  },
});
